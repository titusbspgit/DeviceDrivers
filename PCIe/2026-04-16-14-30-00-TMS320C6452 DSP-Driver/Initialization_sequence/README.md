Initialization sequence feature test for TMS320C6452 GPIO. Builds a bare-metal test that initializes the GPIO, reads PID, sets directions, and exercises a write/readback.

Build: make
Run: ./test_Initialization_sequence (on suitable bare-metal harness or emulator)
